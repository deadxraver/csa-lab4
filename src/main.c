#include <stdio.h>

int main(int argc, char *argv[]) { // TODO: configure formatter to stick * to
                                   // variable type instead of name
  printf("%s\n", "test");
  return 0;
}
